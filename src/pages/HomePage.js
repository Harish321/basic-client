import React from "react";
import Navbar from "../components/Navbar";

const HomePage = () => {
    return (
        <div>
            <Navbar />
            <h1>Cab Management App</h1>
            <p>Select a section to view or manage its data.</p>
        </div>
    );
};

export default HomePage;
